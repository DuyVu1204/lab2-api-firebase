# Backend
fastapi>=0.95
uvicorn[standard]>=0.22
firebase-admin>=6.0.0
google-cloud-firestore>=2.10
pydantic>=1.10

# Frontend / client
streamlit>=1.25
requests>=2.28

# Optional / helpers (used in code)
email-validator>=1.3
python-multipart>=0.0.6
Pyrebase4>=4.5.0  # provides `pyrebase` client used by auth flows

# Additional recommended packages
python-dotenv>=1.0  # if you prefer .env for local secrets
